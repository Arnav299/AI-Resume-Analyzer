{
  "name": "ai-resume-analyzer",
  "version": "1.0.0",
  "description": "AI-powered resume analysis and career recommendation portal",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "backend": "cd backend && python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "start:backend": "cd backend && python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000"
  }
}
